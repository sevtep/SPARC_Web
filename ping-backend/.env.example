# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/ping_db
READ_DATABASE_URL=

# SPARC Sources (Optional)
SPARC_WORDGAME_DATA_PATH=/www/wwwroot/game.agaii.org/backend/wordgame-data.json

# Security
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Access Control
ALLOW_GUEST=false

# SMTP (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=
SMTP_TLS=true
SMTP_SSL=false

# Google OAuth (will be configured later)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# SMTP (for email verification - will be configured later)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Application
FRONTEND_URL=http://localhost:3000

# Server Configuration
HOST=0.0.0.0
PORT=12345
RELOAD=False
WORKERS=4
